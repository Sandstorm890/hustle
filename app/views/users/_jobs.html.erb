<h3>Your Jobs: </h3>
<% current_user.jobs.each do |job| %>
    <p><%= link_to(job.employer.company + " - " + start_date(job), user_job_path(current_user, job)) if job.users.include?(current_user) %></p>
<%end%>